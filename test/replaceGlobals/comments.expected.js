/* !
 * ${copyright}
 */

// A module
sap.ui.define(["jquery.sap.global"],
	function() {
		"use strict";
		/**
		 *
		 * @param oParam
		 * @param sContent
		 */
		A.x = function(oParam, sContent) {
			// Hallo, I bims eins Kommentar
			jQuery.unhandled();

			// Hallo, I bims eins Kommentar
			if (jQuery.unhandled()) {
			}

			// Hallo, I bims eins Kommentar
			if (true && false ||
				jQuery.unhandled()) {
			}

			// Hallo, I bims eins Kommentar
			var a = jQuery.unhandled();

			// Hallo, I bims eins Kommentar
			var a = b + jQuery.unhandled.bind();

			// Hallo, I bims eins Kommentar
			var b = [
				123 + 567,
				jQuery.unhandled(),
				String(jQuery.unhandled())
			];

			// Hallo, I bims eins Kommentar
			someStuff(
				abc,
				jQuery.unhandled(),
				jQuery.unhandled()
			);
		};

		return A;
	}, /* bExport= */ true);