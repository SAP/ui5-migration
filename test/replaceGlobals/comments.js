/* !
 * ${copyright}
 */

// A module
sap.ui.define(["jquery.sap.global"],
	function() {
		"use strict";
		/**
		 *
		 * @param oParam
		 * @param sContent
		 */
		A.x = function(oParam, sContent) {
			jQuery.unhandled();

			if (jQuery.unhandled()) {
			}

			if (true && false ||
				jQuery.unhandled()) {
			}

			var a = jQuery.unhandled();

			var a = b + jQuery.unhandled.bind();

			var b = [
				123 + 567,
				jQuery.unhandled(),
				String(jQuery.unhandled())
			];

			someStuff(
				abc,
				jQuery.unhandled(),
				jQuery.unhandled()
			);
		};

		return A;
	}, /* bExport= */ true);
